# Copy this file to .dev.vars and paste your Stripe secret key.
# .dev.vars is used by "npm run dev" only — use a TEST key here.
# Test key: https://dashboard.stripe.com/test/apikeys
#
# For production (real card charges): set the LIVE key in Cloudflare, not here:
#   npx wrangler secret put STRIPE_SECRET_KEY
# Then paste sk_live_... from https://dashboard.stripe.com/apikeys (Live mode).
STRIPE_SECRET_KEY=sk_test_your_key_here
